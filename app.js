(function(window) {

    'use strict';

    var doc = window.document,
        body = doc.querySelector('body');

    /**
     * App init
     */
    window.onload = function() {

    };

}(window));
